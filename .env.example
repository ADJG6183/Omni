# Database
POSTGRES_USER=omni
POSTGRES_PASSWORD=omnipass
POSTGRES_DB=omni_db
DATABASE_URL=postgresql://omni:omnipass@localhost:5432/omni_db

# API
API_HOST=0.0.0.0
API_PORT=8000
API_ENV=development

# CORS — comma-separated origins allowed to call the backend
ALLOWED_ORIGINS=chrome-extension://,http://localhost:3000
